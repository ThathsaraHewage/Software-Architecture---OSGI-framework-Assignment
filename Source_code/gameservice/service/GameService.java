package gameservice.service;

public interface GameService{
    /*
    * This method implements in the Activator class of the producer
    * Check the number entered by the player
    */
    public boolean checkNumber(String number);
}